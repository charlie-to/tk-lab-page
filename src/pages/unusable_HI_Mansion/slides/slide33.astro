---
import SlideLayout from '@layouts/slide_layout.astro'
//Images
import {Image} from 'astro:assets'
import ghost_teacher from './Images/ghost_teacher.png'
import driving_sim from './Images/driving_sim.jpg'
import flight_sim from './Images/flight_sim.jpg'
---

<SlideLayout>
    <div class="slide-content">
        <div class="textArea" style="position: relative; top: 30px; left: 280px; opacity: 0;">
            <div class="text white" style="display: block; font-size: 24px; opacity: 1; margin-top: 0;">
                高橋・狩川研究室の研究テーマの1つは<br>
                「ヒューマンインターフェース」です
            </div>
            <div class="text white" style="display: block; font-size: 24px; opacity: 1; margin-top: 20px;">
                航空分野などを対象に、ユーザーのミスを防ぎ<br>
                ユーザーを支援するインターフェースの研究を行っています
            </div>
        </div>


        <Image src={ghost_teacher} alt="ghost" class="ghost" style="width: 180px; transform: translate(-50%, -50%); opacity: 0; position: absolute; top: 20%; left: 15%; transition: opacity 1s, width 1s, transform 1s;"/>
        <div class="imageArea"
            style="display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; opacity: 0;">
            <div style="text-align: center; margin: 50px auto;">
                <Image src={driving_sim} alt="driving simlator" style="width: 360px;"/>
                <div><span class="white">ドライブシミュレーター</span></div>
            </div>
            <div style="text-align: center; margin: 50px auto;">
                <Image src={flight_sim} alt="flight simlator" style="width: 360px;"/>
                <div><span class="white">航空管制シミュレーター</span></div>
            </div>
        </div>
        <div class="buttonArea" style="opacity: 0;">
            <button id="next" style="
            top: 93%;
            left: 80%;
            transform: translate(-50%, -50%);
            width: 300px;
            height: 40px;
            
            ">Next →</button>
        </div>
    </div>
    <script>
        window.addEventListener('DOMContentLoaded', () => {
            const text = document.querySelector('.textArea');
            const ghost = document.querySelector('.ghost');
            const image = document.querySelector('.imageArea');
            const button = document.querySelector('.buttonArea');

            setTimeout(() => {
                text.style.transition = "opacity 2s ease-in-out"
                image.style.transition = "opacity 2s ease-in-out"
                ghost.style.transition = "opacity 2s ease-in-out"
                text.style.opacity = 1;
                image.style.opacity = 1;
                ghost.style.opacity = 1;
            }, 500);

            setTimeout(() => {
                button.style.transition = "opacity 2s ease-in-out"
                button.style.opacity = 1;
            }, 6000);

            // ボタンが押されたら画面遷移
            const nextButton = document.getElementById('next');
            nextButton.addEventListener('click', () => {
                const nextPageUrl = './slide34.html';

                // 次のページを読み込む
                window.fetch(nextPageUrl).then((response) => {
                    // 読み込みが完了したら画面遷移を行う
                    window.location.href = nextPageUrl;
                });
            });
        });
    </script>
</SlideLayout>