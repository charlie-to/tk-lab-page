---
import SlideLayout from '@layouts/slide_layout.astro';
---

<SlideLayout>
  <div
    class="slide-content flex items-center justify-center"
    style="position: relative; min-height: 100vh;"
  >
    <div
      class="textArea relative flex flex-col items-center justify-center"
      style="font-size: max(40px, 3.6vw);"
    >
      <div class="text first opacity-100" style="opacity: 0;">
        廊下を進んでいくと
      </div>
      <div class="text second mt-10 opacity-100" style="opacity: 0;">
        不気味な笑い声が聞こえてくる・・・
      </div>
    </div>
  </div>

  <script is:inline>
    window.addEventListener('DOMContentLoaded', () => {
      const textFirst = document.querySelector('.first');
      const textSecond = document.querySelector('.second');

      // 画面の要素を時間差で登場させる
      setTimeout(() => {
        textFirst.style.transition = 'opacity 2s ease-in-out';
        textFirst.style.opacity = 1;
      }, 1000);

      setTimeout(() => {
        textSecond.style.transition = 'opacity 2s ease-in-out';
        textSecond.style.opacity = 1;
      }, 4000);

      setTimeout(() => {
        textFirst.style.opacity = 0;
        textSecond.style.opacity = 0;
      }, 7000);

      // 自動で画面遷移
      setTimeout(() => {
        const nextPageUrl = '../slide9/index.html';

        // 次のページを読み込む
        window.fetch(nextPageUrl).then((response) => {
          // 読み込みが完了したら画面遷移を行う
          window.location.href = nextPageUrl;
        });
      }, 9000);
    });
  </script>
</SlideLayout>
